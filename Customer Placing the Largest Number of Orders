# Write your MySQL query statement below
#select the max of  customer_number to get the largest number in the list group those according to the  customer_number and order them in the decending order so the greatest number comes first limit 1 is to choose the 1st element in the list
SELECT customer_number FROM Orders GROUP BY customer_number ORDER BY count(*) DESC LIMIT 1; 
